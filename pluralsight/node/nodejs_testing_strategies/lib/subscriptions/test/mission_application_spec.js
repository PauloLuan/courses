var assert = require('assert');

describe('Applying for a mission', function() {
  describe('using a valid name, first, last, email, age, height, weight', function() {
    it('let´s them in');
    it('reports a valid name');
    it('reports a valid first');
    it('reports a valid last');
    it('reports a valid email');
    it('reports a valid age');
    it('reports a valid height');
    it('reports a valid weight');
  });
});
